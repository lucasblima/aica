# Product Requirements Document (PRD) - Aica Frontend

## 1. Product Vision
**Aica Life OS** is a holistic "Life Operating System" designed to integrate productivity, emotional well-being, and community leadership. It uses AI to gain deep context about the user's life, proactively organizing tasks, managing communications, and providing emotional support, all while prioritizing **User Privacy and Data Sovereignty**.

**Core Philosophy:**
- **Context-Aware:** The system understands *why* a task exists.
- **Gamified Existence:** Visualizing life as a finite journey ("Life in Weeks").
- **Privacy by Design:** Raw communication logs are processed for insights but NOT stored permanently.
- **Proactive Assistance:** "Aica Auto" suggests priorities and drafts communications.

## 2. Target Audience
- Individuals seeking high-performance productivity balanced with mental health.
- Community leaders managing complex networks.
- Users concerned with data privacy who want AI assistance without surveillance.

## 3. Core Pillars & Features

### 3.1. "Minha Vida" (Life Dashboard)
- **Life in Weeks (Death Clock):** Visual grid of life progress.
- **Life Areas (Modules):** Cards for Finances, Health, Community.
- **Gamification:** Leveling system, Streaks, Efficiency Score.

### 3.2. "Meu Dia" (Daily Execution)
- **Timeline View:** Tasks arranged by time/priority.
- **Aica Auto™:** Intelligent sorting based on extracted context.
- **Pomodoro:** Integrated focus timer.

### 3.3. Communication & Collaboration (Privacy-First)
- **WhatsApp Integration (Stateless):**
  - **Flow:** Evolution API Webhook -> n8n/AI Processing -> Extract Insight/Action -> **Discard Raw Message**.
  - **No Logs:** `pair_conversations` table is REMOVED or used only for *drafts* generated by AI, not incoming history.
  - **Context:** AI relies on "Memories" (summarized facts) rather than raw logs.
- **Contact Network:**
  - Tracks *last interaction date* and *relationship health* based on metadata, without storing message content.

### 3.4. Emotional Intelligence
- **Memories (The "Encrypted" Context):**
  - Instead of storing "I'm sad because X", store structured insights: `{"sentiment": "negative", "trigger": "work_deadline", "timestamp": "..."}`.
  - **Embeddings:** Vector representations of *summarized* context for RAG, allowing the AI to "remember" patterns without reading old chats.

## 4. Technical Architecture

### 4.1. Source of Truth
- **Supabase is the Master:** For Tasks, Contacts, Memories, and User Stats.
- **Evolution API is a Gateway:** Pass-through for messages.
- **Plane (Legacy):** Removed as a dependency. Aica is self-contained.

### 4.2. Data Flow (Privacy)
1. **Incoming Message:** Webhook hits n8n.
2. **AI Processing:** Gemini analyzes intent (e.g., "Client asking for meeting").
3. **Extraction:**
   - Create `work_item`: "Meeting with Client".
   - Create `memory`: "Client anxious about deadline".
   - Update `contact_network`: Last interaction = Now.
4. **Purge:** Raw message payload is discarded from memory.

### 4.3. Database Schema (Target ~15 Tables)
- **Core:** `users`, `profiles`, `workspaces`, `associations`, `modules`, `work_items`, `states`.
- **Gamification:** `user_stats`, `task_metrics`, `activity_log`.
- **Context:** `memories` (includes embeddings), `daily_reports`.
- **Network:** `contact_network` (metadata only).

## 5. Roadmap

### Phase 1: Foundation (Done)
- UI Redesign & Basic Supabase connection.

### Phase 2: Cleanup & Privacy (Current)
- [ ] Schema Cleanup (Drop 30+ tables).
- [ ] Implement Privacy-First Webhook logic.

### Phase 3: Gamification & Visualization
- [ ] Life Grid & Priority Matrix components.

### Phase 4: AI & Voice
- [ ] Voice interface & Sentiment analysis.
