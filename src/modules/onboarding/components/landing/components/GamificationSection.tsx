/**
 * GamificationSection — CP categories + Streaks.
 * Stub placeholder — will be implemented in Task 7.
 */
export function GamificationSection() {
  return (
    <section className="py-24 px-6 flex items-center justify-center min-h-[50vh]">
      <p className="text-ceramic-text-secondary text-lg">Gamification Section — Coming Soon</p>
    </section>
  );
}
