import React from 'react';
import { AuthLoadingScreen } from './AuthLoadingScreen';

/**
 * Example usage of AuthLoadingScreen component
 *
 * This example shows how the component looks in isolation.
 * In production, it will be used in App.tsx while checking authentication.
 */
export const AuthLoadingScreenExample: React.FC = () => {
  return <AuthLoadingScreen />;
};

export default AuthLoadingScreenExample;
